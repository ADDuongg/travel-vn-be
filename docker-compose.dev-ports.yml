# Chỉ dùng khi cần truy cập Mongo/Redis từ host (Compass, redis-cli, debug).
# docker compose -f docker-compose.yml -f docker-compose.dev-ports.yml up -d

services:
  mongo:
    ports:
      - "27017:27017"

  redis:
    ports:
      - "6379:6379"
