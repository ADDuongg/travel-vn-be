# Promtail config — LOCAL.
#
# Strategy: discover container qua Docker socket, đọc stdout/stderr trực tiếp.
# Nest app chạy ngoài Docker (yarn start:dev) sẽ KHÔNG bắt được tại local;
# chỉ thu được mongo/redis/elasticsearch container logs.
#
# Cardinality: chỉ giữ label bounded (env, container, compose_service, stream, level, service).
# requestId/userId luôn để dạng JSON field, query bằng `| json` trong LogQL.

server:
  http_listen_port: 9080
  grpc_listen_port: 0
  log_level: info

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker

    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s

    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'

      - target_label: 'job'
        replacement: 'docker'

      - source_labels:
          ['__meta_docker_container_label_com_docker_compose_service']
        target_label: 'compose_service'

      - source_labels:
          ['__meta_docker_container_label_com_docker_compose_project']
        target_label: 'compose_project'

      - source_labels: ['__meta_docker_container_log_stream']
        target_label: 'stream'

      - target_label: 'env'
        replacement: 'local'

      # Đảm bảo promtail biết chính xác path log file container.
      - source_labels: ['__meta_docker_container_id']
        target_label: '__path__'
        replacement: '/var/lib/docker/containers/$1/*.log'

      - source_labels: ['__meta_docker_container_name']
        regex: '/(monitoring-local-promtail|monitoring-local-loki)'
        action: drop

    pipeline_stages:
      - docker: {}

      # Safety net: Loki yêu cầu stream phải có >= 1 label.
      - static_labels:
          source: docker

      - json:
          expressions:
            level: level
            app_service: service
            app_environment: environment
            app_version: version

      # - labels:
      #     level:
      #     app_service:
      #     app_environment:
